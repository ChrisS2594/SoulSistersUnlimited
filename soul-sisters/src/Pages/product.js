import React from "react";
import "./../App.scss";
import {
    BrowserRouter as Router,
    Link,

    //BrowserHistory
} from "react-router-dom";
function Crafts() {
    return (
        <div>
          {/* I will be building containers in which images can be called by an api or from DB based on client uploads of products. */}
         

        </div>

    );
}

export default Crafts;